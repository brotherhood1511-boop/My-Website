# Backend

Empty for now. The site currently runs entirely on static data files in
`frontend/src/data/`, per the "static now, Supabase later" decision in
`../design-plan.md`.

When it's time for a real database, this is where the Supabase
schema/migrations (or an API server, if that route is chosen instead)
will live.
